{
  "name": "cv",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "wmr --port=3000 --host",
    "build": "wmr build && yarn backend",
    "backend": "node db.js && json-server --watch db.json --port=8080"
  },
  "alias": {
    "react": "preact/compat",
    "react-dom": "preact/compat"
  },
  "devDependencies": {
    "wmr": "^3.7.2"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "nanoid": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.53.0"
  }
}
